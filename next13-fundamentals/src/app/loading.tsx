export default function Loading() {
  return <p> Carregando... </p>
}

// se crio loading na raiz do meu projeto, ele contará para todos os componentes
// se crio o arquivo loading em outra rota, ele irá substituir o loading da raiz
